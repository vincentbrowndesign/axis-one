"use client";

import AxisCameraInstrument from "@/components/axis/axis-camera-instrument";

export default function AxisPage() {
return (
<main className="min-h-screen bg-black text-white">
<AxisCameraInstrument />
</main>
);
}